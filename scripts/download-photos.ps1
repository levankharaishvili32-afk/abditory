# This script is no longer used.
# The download is done by DOWNLOAD-PHOTOS.bat using curl.exe instead.
# Kept only so nothing breaks if something still points at this path.
Write-Host 'Use DOWNLOAD-PHOTOS.bat instead.'
